package com.example.musicplayer.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class WebConfig {
    // This class is intentionally empty since CORS is now handled in SecurityConfig
}
